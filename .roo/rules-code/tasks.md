# Tasks

This document provide instructions on how to manage project tasks under `docs/tasks` during implementation phase.

- When instructed to implement tasks, consider the `docs/specs/netmonitor.md` for context.
- Create a git commit after implementing each task.
